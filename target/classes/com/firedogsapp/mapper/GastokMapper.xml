<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.firedogsapp.mapper.GastokMapper">
	<select id = "test" resultType = "String">
		select
		device_id
		FROM KRC_DEVICE
		WHERE office_id = '10868'
	</select>
	
	<insert id = "insert_device_status">
		INSERT KRC_DEVICE_STATUS(device_id, service_id, reg_date, last_recv, remain1) values(#{device_id}, 'FD', now(), now(), '10')
	</insert>

</mapper>